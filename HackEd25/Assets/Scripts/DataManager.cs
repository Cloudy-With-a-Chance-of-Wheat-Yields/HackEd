using UnityEngine;

public class DataManager : MonoBehaviour
{
    public float[] fltMaxTemp = new float[13];
    public float[] fltMinTemp = new float[13];
    public float[] fltRain = new float[13];
    public float[] fltRainTime = new float[13];


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int i = 1; i< 13;i++)
        {



        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
